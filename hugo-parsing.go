package main

/*
import (
	"fmt"
	"log"
	"os/exec"
)

func init() {
	hugocmd := exec.Command("hugo", "-D")
	hugocmd.Dir = "hugo"
	fmt.Println("runnning hugo to parse templates...")

	out, err := hugocmd.Output()
	if err != nil {
		log.Fatal(err)
	}

	fmt.Printf("%s\n", out)

}
*/
